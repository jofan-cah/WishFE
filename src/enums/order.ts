export enum OrderType {
  print = 'DICOM ke 3D Print',
  stl = 'DICOM ke STL',
}

export enum OrderStatus {
  pending = 'Pending',
  confirm = 'Confirm',
  receipt = 'Receipt',
  review = 'Review',
  delivery = 'Delivery',
  build = 'Build',
  complete = 'Complete',
  cancel = 'Cancel',
}

export enum OrderCourierType {
  jnt = 'J&T Express',
  gosend = 'Go Send'
}

export enum UsersGender {
  male = 'Male',
  female= 'Female'
}


